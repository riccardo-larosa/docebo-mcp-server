# MCP Server Environment Variables
# Copy this file to .env and fill in the values

# Docebo instance URL (required)
API_BASE_URL=https://your-instance.docebosaas.com

# Server configuration
PORT=3000

# OAuth credentials (required for stdio transport, optional for HTTP token proxy)
DOCEBO_CLIENT_ID=your_client_id
DOCEBO_CLIENT_SECRET=your_client_secret
DOCEBO_USERNAME=your_username
DOCEBO_PASSWORD=your_password

# Streamable HTTP OAuth resource server (set to enable remote MCP)
# MCP_SERVER_URL=https://your-public-url.example.com

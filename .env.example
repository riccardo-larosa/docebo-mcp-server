# MCP Server Environment Variables
# Copy this file to .env and fill in the values

# Server configuration
PORT=3000
LOG_LEVEL=info

# API Authentication
BEARER_TOKEN_BEARERAUTH=your_bearer_token_here

# OAuth credentials for stdio transport (used instead of BEARER_TOKEN_BEARERAUTH)
DOCEBO_CLIENT_ID=your_client_id
DOCEBO_CLIENT_SECRET=your_client_secret

# Add any other environment variables your API might need
